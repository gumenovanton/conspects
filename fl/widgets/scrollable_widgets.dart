// SingleChildScrollView - to get a single child scrolable, for articles, rare to use, especially for various widgets and i should be aware of it

// ListView.builder() - used to create scralable list of equal witgets

// ListView.separated() - used to create scralable list of equal witgets with separator widget

// PageView - scrolling by page

// StaggeredGridView - almost similar to html grids

// CustomScrollView - scrollable widget, unlike ListView and GreedView it can contains different witgets(slivers)

// SliverAppBar - top sliver for CustomScrellView, used for headers

// SliverToBoxAdapter - sliver with multi elements

// GridView.builder() - to create adaptive grid view with wrap on shrink

// SliverGridDelegateWithFixedCrossAxisCount - create frid layout with fixed column(if vertical) or rows(if horisontal) number

// Dismesible - wraps a child and i cont delete it with swipe left, used in listViews, wraps listTil

// layoutBuilder - used to show multiple layouts depending on window size

// FractionallySizedBox - wrap a children and tekes widthFactor or hightFactor in procentage of parent, like element size in procentage in css, i can place it into Flexible to use in Row or Column

// Flexible - used in columns and rows, i can set flex property and sew width or hight of an element regardeng to each other
