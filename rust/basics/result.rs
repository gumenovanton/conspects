Result
1. Проверка состояния (Boolean)
• is_ok(): true, если результат Ok.
• is_err(): true, если результат Err.
• is_ok_and(f): true, если это Ok и значение соответствует условию f.
• is_err_and(f): true, если это Err и ошибка соответствует условию f.
2. Работа со ссылками (References)
• as_ref(): Преобразует &Result<T, E> в Result<&T, &E>.
• as_mut(): Преобразует &mut Result<T, E> в Result<&mut T, &mut E>.
• as_deref(): Преобразует Ok(T) в Ok(&T::Target) (например, Result<String, E> -> Result<&str, E>).
• as_deref_mut(): Аналогично для мутабельных ссылок.
3. Извлечение значения (Unwrapping)
• unwrap(): Возвращает Ok, паникует при Err.
• expect(msg): Возвращает Ok, паникует с вашим сообщением при Err.
• unwrap_or(default): Возвращает Ok или значение default.
• unwrap_or_else(f): Возвращает Ok или результат функции f(E) (принимает ошибку).
• unwrap_or_default(): Возвращает Ok или дефолтное значение типа T.
• unwrap_err(): Возвращает ошибку, паникует при Ok.
• expect_err(msg): Возвращает ошибку, паникует с сообщением при Ok.
4. Трансформация (Mapping)
• map(f): Изменяет значение внутри Ok.
• map_err(f): Изменяет значение внутри Err (часто используется для конвертации типов ошибок).
• map_or(default, f): Применяет f к Ok или возвращает default.
• map_or_else(default_f, f): Применяет f к Ok или вычисляет дефолт через default_f(E).
5. Цепочки вызовов (Control Flow)
• and(res): Возвращает res, если первый результат Ok.
• and_then(f): (FlatMap) Вызывает функцию f, если результат Ok. Позволяет соединять несколько
функций, возвращающих Result.
• or(res): Возвращает первый результат, если он Ok, иначе возвращает res.
• or_else(f): Вызывает f, если результат Err. Позволяет попытаться исправить ошибку.
6. Работа с побочными эффектами (Inspection)
• inspect(f): Выполняет функцию f(&T), если результат Ok, и прокидывает Result дальше.
• inspect_err(f): Выполняет функцию f(&E), если результат Err. Идеально для логирования ошибок без
их обработки.
7. Конвертация
• ok(): Превращает Result<T, E> в Option<T> (ошибка теряется).
• err(): Превращает Result<T, E> в Option<E> (успех теряется).
• transpose(): Меняет местами Result и Option: Result<Option<T>, E> -> Option<Result<T, E>>.
• flatten(): Раскрывает вложенность Result<Result<T, E>, E> в один Result<T, E>.
8. Итераторы
• iter(): Итератор по значению в Ok (0 или 1 элемент).
• iter_mut(): Мутабельный итератор.
• into_iter(): Превращает Result в итератор, потребляя его.